# RECOMMENDED: Use 'mamba' instead of 'conda' for 10-50x faster environment creation
# Install Miniforge3 (includes mamba): /mnt/s/Commesse/00-09_System/scripts/install-mambaforge.sh
# Create environment: mamba env create -f environment.yml
# Update environment: mamba env update -f environment.yml
#
name: tracecolor-github-repo
channels:
  - conda-forge
  - conda-forge
dependencies:
  - python=3.12
  - colorlog>=6.0.0  # Install via mamba or conda
  - pytest           # Install via mamba or conda
  - pytest-cov       # Install via mamba or conda
  - pip              # Keep pip for any pip-only packages if needed later
